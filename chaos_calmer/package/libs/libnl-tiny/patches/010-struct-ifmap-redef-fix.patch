--- a/include/linux/if.h
+++ b/include/linux/if.h
@@ -116,6 +116,7 @@ enum {
  *	being very small might be worth keeping for clean configuration.
  */
 
+/*
 struct ifmap 
 {
 	unsigned long mem_start;
@@ -124,8 +125,10 @@ struct ifmap
 	unsigned char irq;
 	unsigned char dma;
 	unsigned char port;
+*/
 	/* 3 bytes spare */
+/*
 };
-
+*/
 
 #endif /* _LINUX_IF_H */
